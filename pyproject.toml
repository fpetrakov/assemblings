[project]
name = "assemblings"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fpetrakov", email = "jakephin2015@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
assemblings = "assemblings:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
